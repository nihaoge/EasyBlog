<!DOCTYPE html>
<html>
  <head>
    <title>首页</title>
    <%- include head %>
  </head>
  <body>
    <div class="list">

      <% data.list.map((ele, idx)=>{ %>
        <div class="row">
          <span><%= (idx+1) %></span>
          <span><%= ele.username %></span>
          <span><%= ele.title %></span>
          <span><%= ele.createTimeZH %></span>
          <span>
            <a href="">编辑</a>
            <a href="">删除</a>
          </span>
        </div>
      <% }) %>

      <div class="pages">
        <% for(let i=1; i<=data.total; i++) { %>
          <a href="/?page=<%= i %>"><%= i %></a>
        <% } %>
      </div>
    </div>
  </body>
</html>
